resources:
  jobs:
    sample_job:
      name: sample_job
      environments:
        - environment_key: default
          spec:
            environment_version: '2'
            dependencies:
              - brick_by_brick
      tasks:
        - task_key: sample_job_task
          environment_key: default
          python_wheel_task:
            package_name: brick_by_brick
            entry_point: main
